#include <stdio.h>
#include <string.h>
int main()
{
    // char c[10];
    // printf("Enter name :");
    // scanf("%s",&c);
    // printf("count=%d",strlen(c));
    int count = 500;
    for (int i = 0; i < count; i++)
    {
        printf("i love you ❤️");
    }

    return 0;
}